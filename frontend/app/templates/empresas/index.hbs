<div class="panel panel-info">
    <div class="panel-heading">
        <h2>
            <i class="fa fa-building-o"></i>
            Empresas
            <span class="pull-right">
                {{#link-to 'empresas.nova'}}<i class="fa fa-plus"></i>{{/link-to}}
            </span>
        </h2>
    </div>
    <div class="panel-body">
        <table class="table">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Telefone</th>
                    <th>E-mail</th>
                    <th>Endereço</th>
                    <th class="actions"></th>
                </tr>
            </thead>
            <tbody>
                {{#each model as |empresa|}}
                <tr>
                    <td>{{empresa.nome}}</td>
                    <td>{{empresa.telefone}}</td>
                    <td>{{empresa.email}}</td>
                    <td>{{empresa.endereco}}</td>
                    <td>
                        {{#link-to 'empresas.editar' empresa.id class='btn btn-sm btn-default' active=false}}<i class="fa fa-pencil"></i>{{/link-to}}
                        <button {{action 'remove' empresa}} class='btn btn-sm btn-danger' active=false}}><i class="fa fa-times"></i></button>
                    </td>
                </tr>
                {{else}}
                <tr class="empty text-center">
                    <td colspan="5">Nenhuma empresa foi encontrada.</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>